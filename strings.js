const word = "beautiful";
const result = word.slice(0,-5,-6,-7,-8);
console.log(result);

String.prototype.insert = function(index,string){
      if (index > 0)
      {
        return Food.substring(0,index) + food.substring()
      }
}


